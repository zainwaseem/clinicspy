import { RiseLoader } from "react-spinners";

const Loading = () => {
  return (
    <div className="flex justify-center items-center p-5 h-screen mt-5">
      <RiseLoader color="#1ABBD9" />
    </div>
  );
};

export default Loading;
